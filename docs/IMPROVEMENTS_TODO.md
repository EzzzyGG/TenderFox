Похоже, что вы уже успешно интегрировали большую часть веток в `main` (по дереву: `docs/*`, `web/*`, крупный `scheduler.py`, авторизация/привязка Telegram, миграции Alembic и т.д.). Ниже — **максимально подробная TODO** по улучшениям, ориентированная на текущую архитектуру (FastAPI + Postgres + Redis + Scheduler + Telegram bot) и на то, что вы хотите довести до стабильного MVP.

---

# TenderFox — TODO улучшений (максимально подробно)

## 0) Контекст: что сейчас вливаем из веток

**Вливаем (сейчас в PR):**
- `chore/docs-reorg` — реорганизация документации (уже в `main`, но PR закрепляет README/доки как единый источник)
- `feat/landing-web` — статический лендинг + web-сервис в Docker Compose
- `feat/scheduler-bot-userid` — улучшения scheduler + Telegram bot verify flow через pending phones
- `feat/pr21-auth-cleanup` — Alembic 0005, модель `TelegramPendingPhone`, правки scheduler/bot, чистка моделей
- `feat/remove-legacy-chat-id` — Alembic 0006 и окончательная чистка legacy `chat_id`

**Не вливаем:**
- `docs/master-plan`
- `feat/scheduler-delivery`

---

## 1) Critical / Security (сделать в первую очередь)

### 1.1 Ограничить брутфорс и спам по auth/verify
- **Где:** `app/api/routers/auth.py`
- **Что сделать:**
  1. Добавить лимит попыток ввода кода (по phone+IP, хотя бы по phone).
  2. Добавить задержку/блокировку после N неверных попыток.
  3. Добавить TTL на verification попытки.
- **Критерий готовности:**
  - После 5 неверных попыток — дальнейшие попытки блокируются на 10–15 минут.

### 1.2 Секреты: убрать хранение критичных секретов в plain `.env` для продакшна
- **Где:** `docs/DEPLOY_RU_YC.md`, `docker-compose.yml`
- **Что сделать:**
  1. Добавить раздел про Docker secrets / Secret Manager.
  2. Добавить пример `docker-compose.prod.yml` (опционально) с secrets.
- **Критерий готовности:**
  - В деплой-доках явно описано, как хранить `TELEGRAM_BOT_TOKEN`, JWT секреты и DB пароль.

### 1.3 Telegram: минимальная защита от флуда
- **Где:** `app/integrations/telegram/bot.py`
- **Что сделать:**
  1. Добавить rate limit на команды `/verify` и на обработку контакта.
  2. Не логировать телефон целиком (маскировать).
- **Критерий готовности:**
  - Один пользователь не может спамить `/verify` чаще, чем раз в N секунд.

---

## 2) Миграции / БД (Alembic)

### 2.1 Проверить порядок миграций и совместимость
- **Где:** `alembic/versions/0004_telegram_pending_phones.py`, `0005_subscriptions_user_id_not_null.py`, `0006_drop_subscriptions_chat_id.py`
- **Что сделать:**
  1. Прогнать миграции на чистой БД: `alembic upgrade head`.
  2. Прогнать на БД с legacy данными (если есть) и проверить backfill в 0005.
- **Критерий готовности:**
  - Миграции проходят без ручных вмешательств и без ошибок.

### 2.2 Индексы и ограничения
- **Где:** `app/models/subscription.py` + миграции
- **Что сделать:**
  1. Добавить уникальность подписок хотя бы на (user_id, keyword, region) или валидировать на уровне сервиса.
  2. Добавить CHECK constraints для min_price/max_price (min <= max).
- **Критерий готовности:**
  - Нельзя создать дублирующую подписку.

---

## 3) Scheduler & ingestion

### 3.1 Retry/backoff для внешних запросов
- **Где:** `app/services/scheduler.py`, `app/sources/gosplan/client.py`
- **Что сделать:**
  1. Внедрить retries с exponential backoff на 429/5xx.
  2. Явно задать таймауты и лимит параллелизма.
- **Критерий готовности:**
  - При временной ошибке Gosplan — scheduler не падает и повторяет запрос.

### 3.2 Транзакции и commit
- **Где:** `app/services/scheduler.py`
- **Что сделать:**
  1. Не держать длинную транзакцию на всю рассылку.
  2. Коммитить по подписке или батчами.
- **Критерий готовности:**
  - При падении на середине цикла не откатываются все уже отправленные deliveries.

### 3.3 Дедуп и конкурентный запуск
- **Где:** `app/models/delivery.py`, `app/services/scheduler.py`
- **Что сделать:**
  1. Добавить уникальный индекс (subscription_id, tender_id) на deliveries.
  2. Обрабатывать конфликт insert (ON CONFLICT DO NOTHING).
- **Критерий готовности:**
  - Два scheduler процесса не создают дубль доставок.

---

## 4) Telegram bot

### 4.1 Очистка просроченных pending phones
- **Где:** `app/integrations/telegram/bot.py` или отдельный джоб/cron
- **Что сделать:**
  1. Добавить периодическую очистку `telegram_pending_phones` по `expires_at`.
- **Критерий готовности:**
  - Таблица не растёт бесконечно.

### 4.2 Улучшить UX сообщений
- **Где:** `app/integrations/telegram/bot.py`
- **Что сделать:**
  1. Чёткие тексты ошибок.
  2. Подсказки, что делать дальше.
- **Критерий готовности:**
  - Пользователь проходит привязку без “тупиков”.

---

## 5) API (эндпоинты/валидация)

### 5.1 Валидация Subscription фильтров
- **Где:** `app/api/routers/subscriptions.py`, `app/schemas/subscription.py`
- **Что сделать:**
  1. Проверять min_price <= max_price.
  2. Нормализовать keyword/region.
- **Критерий готовности:**
  - Невалидные подписки не создаются.

### 5.2 Добавить pagination
- **Где:** `app/api/routers/search.py`
- **Что сделать:**
  1. Cursor/offset pagination.
- **Критерий готовности:**
  - Поиск возвращает предсказуемые страницы.

---

## 6) DevOps/CI

### 6.1 CI: интеграционные тесты с Postgres
- **Где:** `.github/workflows/ci.yml`
- **Что сделать:**
  1. Добавить service postgres.
  2. Прогон миграций в CI.
- **Критерий готовности:**
  - Тесты бегают на реальной БД.

### 6.2 Healthchecks
- **Где:** `docker-compose.yml`
- **Что сделать:**
  1. Добавить healthcheck для `api`, `redis`, `scheduler` (минимально).
- **Критерий готовности:**
  - docker compose показывает health.

---

## 7) Документация

### 7.1 Единственная точка правды по докам
- **Где:** `README.md`, `docs/*`
- **Что сделать:**
  1. Убедиться, что ссылки идут на `docs/`, а не на корневые старые файлы.
- **Критерий готовности:**
  - Нет дублей `PLAN.md` в корне.

---

## 8) Наблюдаемость (логирование/метрики)

### 8.1 Метрики scheduler
- **Где:** `app/services/scheduler.py`
- **Что сделать:**
  1. Счётчики: найдено тендеров, отправлено, ошибок.
  2. Экспорт Prometheus (опционально отдельный endpoint).
- **Критерий готовности:**
  - Можно увидеть “сколько отправили” без чтения логов.

---

## 9) Тесты

### 9.1 Тесты auth/telegram verify
- **Где:** `tests/` (новые тесты)
- **Что сделать:**
  1. Unit-тесты нормализации телефона.
  2. Integration-тесты на auth verify flow.
- **Критерий готовности:**
  - Основные сценарии покрыты тестами.

---

# После мержа: удалить ветки

После того как PR будет влит в `main`, можно удалить ветки:
- `chore/docs-reorg`
- `feat/landing-web`
- `feat/scheduler-bot-userid`
- `feat/pr21-auth-cleanup`
- `feat/remove-legacy-chat-id`

(Не трогаем: `main`.)
